DROP TABLE IF EXISTS folder;
CREATE TABLE folder (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    name TEXT NOT NULL
);


ALTER TABLE notes
    ADD COLUMN folder INTEGER REFERENCES folder(id) ON DELETE CASCADE NOT NULL;